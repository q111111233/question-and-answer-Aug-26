import { questionNoAnswer } from 'question-and-answer/helpers/question-no-answer';
import { module, test } from 'qunit';

module('Unit | Helper | question no answer');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = questionNoAnswer([42]);
  assert.ok(result);
});

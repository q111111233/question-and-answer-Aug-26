<h1>Favorite Questions:</h1>
<ul>
  {{#each favoriteQuestions.questions as |question|}}
  <li class="{{if question.onTeam 'taken'}}">
    {{question.content}}
    <button class="btn btn-danger" {{action 'remove' question}}>remove</button>
  </li>
  {{/each}}
</ul>
